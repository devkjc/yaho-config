spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/devkjc/yaho-config/
          search-paths: yaho, yahoo
        encrypt:
          enabled: false
server:
  port: 1230

